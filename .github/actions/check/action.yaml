name: Check project

runs:
  using: composite
  steps:
    - uses: oven-sh/setup-bun@v2
      with:
        bun-version: 1.4.2
    - shell: bash
      run: bun ci
    - shell: bash
      run: bun run check
    - shell: bash
      run: bun run test
